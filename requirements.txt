# Core dependencies
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
tqdm>=4.65.0

# OCR
# PaddlePaddle - installer séparément avec:
# GPU (CUDA 12.9): python -m pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu129/
# CPU only:        python -m pip install paddlepaddle==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
paddleocr>=2.7.0
PyPDF2>=3.0.0

# NLP
nltk>=3.8.0

# API
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# MLOps (optional)
# dvc>=3.0.0  # Uncomment to use DVC
# dvc[s3]     # For S3 remote storage
# dvc[gs]     # For Google Cloud Storage
